<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Infusionist - Inside-Out Flavour</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
            background: #0a0a0a;
            color: #e8e3db;
            overflow-x: hidden;
            line-height: 1.6;
        }

        .grain {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            opacity: 0.03;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
            z-index: 1000;
        }

        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 2rem 4rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 100;
            background: linear-gradient(180deg, rgba(10,10,10,0.95) 0%, rgba(10,10,10,0) 100%);
            backdrop-filter: blur(10px);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 600;
            letter-spacing: 0.1em;
            color: #e8e3db;
        }

        .nav-links {
            display: flex;
            gap: 3rem;
            list-style: none;
        }

        .nav-links a {
            color: #a39b8b;
            text-decoration: none;
            font-size: 0.95rem;
            letter-spacing: 0.05em;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 1px;
            background: #d4a574;
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            color: #e8e3db;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 2rem;
            position: relative;
        }

        .hero-title {
            font-size: clamp(3rem, 12vw, 9rem);
            font-weight: 700;
            letter-spacing: 0.02em;
            margin-bottom: 2rem;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 1.2s ease forwards 0.2s;
        }

        .hero-subtitle {
            font-size: clamp(1.2rem, 3vw, 2rem);
            font-weight: 300;
            letter-spacing: 0.15em;
            color: #d4a574;
            text-transform: uppercase;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 1.2s ease forwards 0.5s;
        }

        .hero-description {
            font-size: clamp(1rem, 2vw, 1.3rem);
            max-width: 600px;
            margin: 3rem auto 4rem;
            color: #a39b8b;
            line-height: 1.8;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 1.2s ease forwards 0.8s;
        }

        .cta-button {
            padding: 1.2rem 3rem;
            background: transparent;
            border: 1px solid #d4a574;
            color: #d4a574;
            font-size: 1rem;
            letter-spacing: 0.1em;
            cursor: pointer;
            transition: all 0.4s ease;
            text-transform: uppercase;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 1.2s ease forwards 1.1s;
            position: relative;
            overflow: hidden;
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(212, 165, 116, 0.1), transparent);
            transition: left 0.6s ease;
        }

        .cta-button:hover::before {
            left: 100%;
        }

        .cta-button:hover {
            background: rgba(212, 165, 116, 0.1);
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(212, 165, 116, 0.2);
        }

        .scroll-indicator {
            position: absolute;
            bottom: 3rem;
            left: 50%;
            transform: translateX(-50%);
            opacity: 0;
            animation: fadeIn 1s ease forwards 1.5s;
        }

        .scroll-indicator::before {
            content: '';
            display: block;
            width: 1px;
            height: 60px;
            background: linear-gradient(180deg, transparent, #d4a574);
            margin: 0 auto 1rem;
            animation: scrollLine 2s ease-in-out infinite;
        }

        @keyframes scrollLine {
            0%, 100% { transform: scaleY(0.3); opacity: 0.3; }
            50% { transform: scaleY(1); opacity: 1; }
        }

        section {
            min-height: 100vh;
            padding: 8rem 4rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .content-section {
            max-width: 1400px;
            margin: 0 auto;
        }

        .section-title {
            font-size: clamp(2.5rem, 6vw, 5rem);
            font-weight: 600;
            margin-bottom: 3rem;
            letter-spacing: 0.02em;
        }

        .section-subtitle {
            font-size: clamp(1rem, 2vw, 1.5rem);
            color: #d4a574;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            margin-bottom: 1rem;
            font-weight: 400;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 4rem;
            margin-top: 4rem;
        }

        .card {
            padding: 3rem;
            background: rgba(212, 165, 116, 0.03);
            border: 1px solid rgba(212, 165, 116, 0.1);
            transition: all 0.5s ease;
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 1px;
            background: linear-gradient(90deg, transparent, #d4a574, transparent);
            transition: left 0.8s ease;
        }

        .card:hover::before {
            left: 100%;
        }

        .card:hover {
            background: rgba(212, 165, 116, 0.05);
            border-color: rgba(212, 165, 116, 0.3);
            transform: translateY(-10px);
        }

        .card h3 {
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            color: #e8e3db;
        }

        .card p {
            color: #a39b8b;
            line-height: 1.8;
            font-size: 1.05rem;
        }

        .philosophy-section {
            background: linear-gradient(180deg, #0a0a0a 0%, #1a1612 50%, #0a0a0a 100%);
        }

        .approach-list {
            list-style: none;
            margin-top: 3rem;
        }

        .approach-list li {
            padding: 2rem 0;
            border-bottom: 1px solid rgba(212, 165, 116, 0.1);
            font-size: 1.3rem;
            color: #a39b8b;
            transition: all 0.3s ease;
            cursor: default;
        }

        .approach-list li:hover {
            color: #e8e3db;
            padding-left: 2rem;
        }

        .approach-list li::before {
            content: '→';
            margin-right: 1.5rem;
            color: #d4a574;
            transition: transform 0.3s ease;
            display: inline-block;
        }

        .approach-list li:hover::before {
            transform: translateX(10px);
        }

        .founders-section {
            background: #0a0a0a;
        }

        .founders-grid {
            display: flex;
            gap: 2rem;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 4rem;
        }

        .founder-card {
            text-align: center;
            padding: 2rem;
            min-width: 200px;
        }

        .founder-card h3 {
            font-size: 2rem;
            margin-bottom: 0.5rem;
            color: #d4a574;
        }

        .quote-section {
            text-align: center;
            font-size: clamp(1.5rem, 3vw, 2.5rem);
            font-weight: 300;
            line-height: 1.6;
            max-width: 900px;
            margin: 0 auto;
            color: #e8e3db;
            font-style: italic;
        }

        footer {
            padding: 4rem;
            text-align: center;
            border-top: 1px solid rgba(212, 165, 116, 0.1);
            background: #0a0a0a;
        }

        footer p {
            color: #a39b8b;
            letter-spacing: 0.1em;
            font-size: 0.9rem;
        }

        .messy-apron-note {
            margin-top: 2rem;
            font-size: 0.85rem;
            color: #6b6559;
            font-style: italic;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
            }
        }

        @media (max-width: 768px) {
            nav {
                padding: 1.5rem 2rem;
            }

            .nav-links {
                gap: 1.5rem;
            }

            section {
                padding: 4rem 2rem;
            }

            .grid {
                gap: 2rem;
            }

            .card {
                padding: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="grain"></div>
    
    <nav>
        <div class="logo">INFUSIONIST</div>
        <ul class="nav-links">
            <li><a href="#philosophy">Philosophy</a></li>
            <li><a href="#approach">Approach</a></li>
            <li><a href="#founders">Founders</a></li>
            <li><a href="#contact">Connect</a></li>
        </ul>
    </nav>

    <section class="hero">
        <h1 class="hero-title">INFUSIONIST</h1>
        <p class="hero-subtitle">Inside-Out Flavour</p>
        <p class="hero-description">
            Some ideas need time. Some flavours need patience. 
            Built slowly, not because it had to be, but because that was the only way it made sense.
        </p>
        <button class="cta-button">Experience the Difference</button>
        <div class="scroll-indicator"></div>
    </section>

    <section id="philosophy">
        <div class="content-section">
            <p class="section-subtitle">The Idea</p>
            <h2 class="section-title">Infusionist is not about excess.<br>It's about control.</h2>
            <div class="grid">
                <div class="card">
                    <h3>Deeper Flavour</h3>
                    <p>In a market full of surface-level marinades, we go deeper—literally. Building flavour into the meat itself, creating food that's evenly seasoned and quietly distinctive.</p>
                </div>
                <div class="card">
                    <h3>Controlled Process</h3>
                    <p>Time does the work. Controlled temperatures, tested recipes, documented methods. No shortcuts, no extremes—just balance you want to come back to.</p>
                </div>
                <div class="card">
                    <h3>Intentional Design</h3>
                    <p>Fewer items, done properly. Bold without being aggressive. Familiar, but never predictable. Premium, without pretension.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="approach" class="philosophy-section">
        <div class="content-section">
            <p class="section-subtitle">Why Infusion</p>
            <h2 class="section-title">Our process is simple,<br>but never careless.</h2>
            <ul class="approach-list">
                <li>Flavour is introduced from the inside, not layered on top</li>
                <li>Cooking happens at controlled temperatures</li>
                <li>Recipes are tested, adjusted, and documented</li>
                <li>Fewer items, done properly</li>
            </ul>
        </div>
    </section>

    <section class="quote-section philosophy-section">
        <div class="content-section">
            <p>"Good food doesn't need to prove itself loudly. It just needs to be consistent."</p>
        </div>
    </section>

    <section id="founders" class="founders-section">
        <div class="content-section">
            <p class="section-subtitle">The Team</p>
            <h2 class="section-title">Built from nearly a decade<br>of care and craft.</h2>
            <p style="max-width: 700px; margin: 2rem auto; text-align: center; color: #a39b8b; font-size: 1.2rem;">
                Infusionist is a side hustle—but not a casual one. This project reflects our shared belief 
                that quality experiences don't come from speed, but from care.
            </p>
            <div class="founders-grid">
                <div class="founder-card">
                    <h3>Sidharth</h3>
                </div>
                <div class="founder-card">
                    <h3>Christy</h3>
                </div>
                <div class="founder-card">
                    <h3>Rohan</h3>
                </div>
            </div>
            <p style="max-width: 600px; margin: 4rem auto; text-align: center; color: #d4a574; font-size: 1.3rem; font-style: italic;">
                We're not chasing trends.<br>We're building something we believe in.
            </p>
        </div>
    </section>

    <section id="contact">
        <div class="content-section" style="text-align: center;">
            <p class="section-subtitle">Let's Connect</p>
            <h2 class="section-title">If you believe good things<br>take time...</h2>
            <p style="max-width: 700px; margin: 2rem auto; color: #a39b8b; font-size: 1.2rem; line-height: 1.8;">
                We're open to collaborations with people and communities who value 
                process over impulse, craft over shortcuts, and patience over pressure.
            </p>
            <button class="cta-button" style="margin-top: 3rem;">Get in Touch</button>
        </div>
    </section>

    <footer>
        <p>INFUSIONIST</p>
        <p class="messy-apron-note">A concept by The Messy Apron · Bangalore</p>
        <p style="margin-top: 2rem; font-size: 0.75rem;">Built slowly. Served honestly. Designed to last.</p>
    </footer>

    <script>
        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // Intersection Observer for scroll animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        document.querySelectorAll('.card, .approach-list li').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'all 0.8s ease';
            observer.observe(el);
        });
    </script>
</body>
</html>